# ZGC returns unused memory back to the OS, so mill does not occupy so much memory if unused

-XX:+UseZGC
-XX:ZUncommitDelay=30
-XX:ZCollectionInterval=5

# Enable tools like gprofiler

-XX:+EnableDynamicAgentLoading
